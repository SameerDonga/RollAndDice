enum LocationPermissionLevel {
  location,
  locationWhenInUse,
  locationAlways,
}

enum PermissionStatus {
  unknown,
  denied,
  granted,
  restricted,
}

enum ServiceStatus1 {
  unknown,
  disabled,
  enabled,
  notApplicable,
}